dfpires@gmail.com
$Unifacef32

npm run: roda o que tiver cadastrado no package json
cd: acessa pasta


Dependencias:
de desenvolvimento
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.4.10",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.3.5"

express ->  npm install @types/express -D <- (no terminal)

comuns 
    "express": "^4.17.1"

yarn typeorm migration:create -n CreateUsers
npm add bcryptjs// dependencia para instalar criptografia nas senhas
npm add -D @types/bcryptjs
import {hash} from 'bcryptjs'
